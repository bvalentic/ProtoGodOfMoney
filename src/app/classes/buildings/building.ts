import { Employee } from '../people/employees/employee';

export class Building {
    cost: number;
    resellValue: number;
    className: string;
    name: string;
    increaseRate: number;
    employees: Employee[];
}
